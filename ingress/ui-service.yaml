kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: ui-deploy
  name: ui-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    k8s-app: ui-deploy












